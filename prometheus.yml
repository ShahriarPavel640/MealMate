global:
  scrape_interval: 5s # Scrape targets every 5 seconds.
  evaluation_interval: 5s # Evaluate rules every 5 seconds.

scrape_configs:
  - job_name: 'mealmate_backend'
    static_configs:
      - targets: ['backend:5000']
